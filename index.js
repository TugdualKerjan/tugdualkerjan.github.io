// var text = 'TUGDUAL';
// var canvas = document.createElement("canvas");
// var fontSize = 80;
// canvas.height = fontSize;
// // canvas.width= 40;
// var context = canvas.getContext('2d');
// // canvas.getContext.apply
// context.fillStyle    = 'gray';
// // canvas.context.style.setAttribute('background-color', 'gray');
// canvas.style.minHeight = `${fontSize}px`;
// context.setTransform(1, Math.tan(-0.2), 0, 1, 0, 0);
// context.font = `${fontSize}px fantasy`;
// context.fillText(text, 0, fontSize);
// context.transform = "skewY(-20deg)";

// // context.fontSize = "100px";
// // context.shadowColor = "lime";
// console.log(canvas);
// document.getElementById('container').style.backgroundImage = "url(" + canvas.toDataURL("image/png")+ ")";
